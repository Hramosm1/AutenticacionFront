// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    firebase: {
        projectId: 'auth-1c7c7',
        appId: '1:480457543365:web:b02d6e6091cb7bc0da2aac',
        storageBucket: 'auth-1c7c7.appspot.com',
        apiKey: 'AIzaSyDP4dzTu5n59p2Ap0Kye61j992HjPm5-KU',
        authDomain: 'auth-1c7c7.firebaseapp.com',
        messagingSenderId: '480457543365',
    },
    production: false,
    serverUrl: 'http://localhost:4456/'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
